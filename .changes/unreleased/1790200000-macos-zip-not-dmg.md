---
type: Changed
title: The macOS desktop app is a zip you run, not a disk image to install
---

The macOS download used to be a disk image with the app beside an Applications shortcut, which reads like a game to install. It is now `nQuake.app` in a zip: open it and run the installer straight from Downloads, no copying into Applications.
